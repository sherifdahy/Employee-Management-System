<UserControl 
             FlowDirection="RightToLeft"
             x:Class="MyApp.WPF.UserControls.Admin.DailyTransactions.NewTransactionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyApp.WPF.UserControls.Admin.DailyTransactions"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderThickness="0,0,0,0.5" BorderBrush="#0057f6"  >
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                    <Button Style="{DynamicResource BlueFlatButton}"
                            x:Name="NewTransactionBtn"
                            Content="انشاء عهدة" Click="NewTransactionBtn_Click"></Button>
                </StackPanel>
            </Border>

            <ContentControl Grid.Row="1" x:Name="Form"></ContentControl>
        </Grid>
    </ScrollViewer>
</UserControl>
