<UserControl 
             FlowDirection="RightToLeft"
             x:Class="MyApp.WPF.UserControls.Admin.Organizations.EditOrganizationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:local="clr-namespace:MyApp.WPF.UserControls.Admin.Organizations"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Border Grid.Row="0">
            <ContentControl x:Name="FormControl"></ContentControl>
        </Border>

        <StackPanel Grid.Row="4" Width="200" HorizontalAlignment="Right">
            <telerik:RadButton x:Name="SaveChangesBtn" telerik:StyleManager.Theme="Fluent" Content="تحديث البيانات" Click="SaveChangesBtn_Click"></telerik:RadButton>
        </StackPanel>
    </Grid>
</UserControl>
