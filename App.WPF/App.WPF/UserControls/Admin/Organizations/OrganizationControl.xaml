<UserControl
             FlowDirection="RightToLeft"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyApp.WPF.UserControls.Admin.Organizations"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" x:Class="MyApp.WPF.UserControls.Admin.Organizations.OrganizationsControl"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- خلي الجدول يتمدد مع الشاشة -->
        <telerik:RadGridView 
    SelectionUnit="FullRow"
    x:Name="OrganizationDataGrid"
    IsReadOnly="True"
    AutoGenerateColumns="False"
    telerik:StyleManager.Theme="Windows11"
    ShowGroupPanel="False"
    IsFilteringAllowed="False"
    AllowDrop="False"
    CanUserResizeColumns="True"
    Margin="0">

            <telerik:RadGridView.Columns>
                <!-- ID -->
                <telerik:GridViewDataColumn 
            DataMemberBinding="{Binding Id}" 
            Width="1*" 
            Header="المعرف" />

                <!-- Name -->
                <telerik:GridViewDataColumn 
            DataMemberBinding="{Binding Name}" 
            Width="2*" 
            Header="اسم المنظمة" />

                <!-- URL -->
                <telerik:GridViewDataColumn 
            DataMemberBinding="{Binding URL}" 
            Width="3*" 
            Header="الرابط" />

                <!-- Emails Count -->
                <telerik:GridViewDataColumn 
            DataMemberBinding="{Binding Emails.Count}" 
            Width="1.5*" 
            Header="عدد الإيميلات" />

                <!-- Selectors Count -->
                <telerik:GridViewDataColumn 
            DataMemberBinding="{Binding Selectors.Count}" 
            Width="1.5*" 
            Header="عدد المحددات" />

                <!-- Edit Button -->
                <telerik:GridViewDataColumn Width="Auto">
                    <telerik:GridViewDataColumn.CellTemplate>
                        <DataTemplate>
                            <Button 
                telerik:StyleManager.Theme="Crystal"
                Click="EditBtn_Click" 
                ToolTip="Edit"
                Cursor="Hand"
                Background="Transparent"
                BorderBrush="Transparent"
                Padding="0"
                Margin="0"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Image Source="/assets/icons/icons8-edit-64.png" Width="18" Height="18"/>
                                    <TextBlock Text="تعديل" Margin="5,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </telerik:GridViewDataColumn.CellTemplate>
                </telerik:GridViewDataColumn>



                <telerik:GridViewDataColumn Width="Auto">
                    <telerik:GridViewDataColumn.CellTemplate>
                        <DataTemplate>
                            <Button 
                telerik:StyleManager.Theme="Crystal"
                Click="DeleteBtn_Click" 
                ToolTip="حذف"
                Cursor="Hand"
                Background="Transparent"
                BorderBrush="Transparent"
                Padding="0"
                Margin="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" 
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Margin="0">
                                    <Image Source="/assets/icons/icons8-delete-96.png" Width="18" Height="18" Margin="0"/>
                                    <TextBlock Text="حذف" 
                               Margin="3,0,0,0" 
                               VerticalAlignment="Center" 
                               Padding="0"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </telerik:GridViewDataColumn.CellTemplate>
                </telerik:GridViewDataColumn>


            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>

</UserControl>
